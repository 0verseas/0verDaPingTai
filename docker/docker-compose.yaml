version: '3.7'

networks:
  bridge:
    external:
      name: ${NETWORKS}

services:
  school:
    image: nginx:alpine
    container_name: 0verDaPingTai
    restart: unless-stopped
    tty: true
    environment:
      - TZ=Asia/Taipei
    volumes:
      - ./app/dist/:/usr/share/nginx/html/
    networks:
      - bridge
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.school.entrypoints=websecure"
      - "traefik.http.routers.school.tls.certresolver=lets-encr"
      - "traefik.http.routers.school.rule=Host(``)"
